<UserControl x:Class="Circuit_Layout.ExperimentDataTable"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Circuit_Layout"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="SqredHeader">
                <Grid>
                    <TextBlock Text="{Binding Text}"/>
                </Grid>
            </DataTemplate>
        </Grid.Resources>
        <DataGrid Name="dgData" Width="557" CanUserDeleteRows="False" CanUserAddRows="False" CanUserReorderColumns="False" CanUserResizeColumns="False" CanUserResizeRows="False" CanUserSortColumns="False" HorizontalScrollBarVisibility="Hidden" ColumnWidth="70" AutoGeneratedColumns="dgData_AutoGeneratedColumns">
        </DataGrid>
    </Grid>
</UserControl>
